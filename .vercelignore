# Ignore everything except api folder and essential files
**

# Allow API folder
!api/
!api/**

# Allow package files for dependency installation
!package.json
!package-lock.json
!yarn.lock

# Allow Vercel config
!vercel.json

# Allow environment setup
!.env.local
